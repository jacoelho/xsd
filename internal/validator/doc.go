// Package validator validates XML documents against compiled XSD schemas.
// It owns streaming validation sessions, runtime facet-program evaluation, and
// the package-local helpers behind the validation pipeline.
package validator
